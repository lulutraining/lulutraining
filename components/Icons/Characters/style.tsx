import styled from '@emotion/styled';

export const Container = styled.div`
  .characters__wrap {
    width: 100%;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
  }

  .characters__item {
    img {
      width: 100%;
      height: auto;
    }
  }
`;
